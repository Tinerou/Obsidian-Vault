#DataStructures 

**priority Queues $\neq$ FIFO**
Each piece of data associated with a key = priority number.
Respect the key, to keep priority.

Remember:
Implementation has 2 parts:
1) Data structure
2) Algorithms (T(n) and S(n))