NOTE: `alt` = `option` on macOS

`alt+shift+<NUM>` - Switches focused program to workspace NUM
`alt+<NUM>` - Switch to workspace NUM

`alt+slash` - Layout tiles horizontal/vertical
`alt+comma` - Layout tiles accordion (also switches between horizontal/vertical)

